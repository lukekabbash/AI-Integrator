{
  "name": "ai-integrator",
  "version": "0.1.2",
  "private": true,
  "description": "Local-first native control plane for coding agents",
  "packageManager": "npm@10.9.2",
  "scripts": {
    "dev": "node scripts/tauri-dev.mjs",
    "dev:watch": "node scripts/tauri-dev.mjs --watch",
    "dev:web": "npm --prefix apps/desktop run dev",
    "build": "npm --prefix apps/desktop run tauri -- build",
    "build:release": "node scripts/tauri-build-release.mjs",
    "build:local": "npm --prefix apps/desktop run tauri -- build --no-bundle && node scripts/sync-local-release.mjs",
    "build:web": "npm --prefix apps/desktop run build",
    "check:bridge": "node --test scripts/bridge-contract.test.mjs && node scripts/check-bridge-contract.mjs",
    "check:bundled-skills": "node --test scripts/bundled-skills-contract.test.mjs scripts/sync-local-release.test.mjs && node scripts/check-bundled-skills.mjs",
    "check:maintainability": "node --test scripts/maintainability-contract.test.mjs && node scripts/check-maintainability.mjs",
    "check:docs": "node scripts/check-docs.mjs",
    "check": "npm run check:bridge && npm run check:bundled-skills && npm run check:maintainability && npm --prefix apps/desktop run check && cargo check --workspace",
    "test": "npm --prefix apps/desktop run test && cargo test --workspace",
    "format": "npm --prefix apps/desktop run format && cargo fmt --all -- --check",
    "lint": "npm --prefix apps/desktop run lint && cargo clippy --workspace --all-targets -- -D warnings"
  }
}
